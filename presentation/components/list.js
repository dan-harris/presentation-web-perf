import styled from '@emotion/styled';
import { List } from 'spectacle';

/**
 * styled list element
 */
export const ListWithEmphasis = styled(List)`
  list-style-position: 'initial';
  list-style: none;
  position: relative;
  padding: 0 2rem 0 4rem !important;
  margin: 0;
`;
