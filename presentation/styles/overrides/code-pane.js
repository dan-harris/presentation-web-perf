/**
 * overrides for spectacle code pane default theme
 */
export function createCodePane(colors = {}, fonts = {}) {
  return {
    margin: "auto",
    fontSize: "1rem",
    fontWeight: "normal",
    minWidth: "100%",
    maxWidth: 800
  };
}
